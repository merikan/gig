
[clean]
description = "Clean build artifacts"
run = "cargo clean"

[build]
description = "Build the project"
run = "cargo build"

["build:release"]
description = "Build the project with optimizations"
run = "cargo build --release"

[run]
description = "Run the application"
run = "cargo run"

[test]
description = "Run tests"
run = "cargo test"

["test:coverage"]
description = "Run tests with coverage (fails if below 90%)"
run = """
cargo tarpaulin --out html --output-dir target/coverage --fail-under 90
echo "Coverage report generated: target/coverage/tarpaulin-report.html"
"""

[lint]
description = "run all linting"
run = """
echo "running clippy"; cargo clippy
echo "running tombi"; tombi lint mise.toml
echo "running shellcheck"; shellcheck $(git ls-files '*.sh')
echo "running actionlint"; actionlint .github/workflows/*.yml
"""

[fmt]
description = "Format the code"
run = """
  echo "running cargo fmt"; cargo fmt
  echo "running shfmt"; shfmt -w $(git ls-files '*.sh')
"""

["fmt:check"]
description = "Check code formatting without modifying files"
run = """
  echo "running cargo fmt --check"; cargo fmt --check
  echo "running shfmt"; shfmt --diff .
"""

[check]
description = "Check the project for errors"
depends = ["lint", "fmt:check"]
run = """
  echo "running cargo check"; cargo check
"""

[ci]
description = "Run all checks (linting, formatting and tests)"
depends = ["check", "test"]

[docs]
description = "Generate and open documentation"
run = "cargo doc --open"

["mise:install"]
description = "Install tools"
run = "mise install"

["mise:upgrade"]
description = "Upgrade tools version"
run = "mise upgrade --bump --local"
